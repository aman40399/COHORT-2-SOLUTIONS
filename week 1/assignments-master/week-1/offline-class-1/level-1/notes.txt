//for getting length of a string
var_name.length

//--for giving index of first word 
var_namr.indexOf(target_word)

//for giving index of last 
 var_name.lastIndexOf(target_word)

 //want to get slice of a string
 var_name.slice(startinf_index,end_index)

.slice   .substr  both have same function to return part of selected string but here is the catch explaining it with an example 

 let str ="amansingh"
//console.log(str.slice(2,5))
//output- ans

 let str ="amansingh"
//console.log(str.substr(2,5))
//output- ansi

//for replacing somrthingh in a string
var_name.replace("target_word","replacing word")

//for splitting an str into array 
var_name>split("delegator")
delegator is any letter character or word u want to remove form the string 
 
 #const value ="hello forks !"
 #cont container = value.split(" ")
 console.log(container)
 //output- [ "hello", "forks","!" ]

//for triming your string
var_name.trim()
remove the extra spaces form the string


//for making your sring in uppercase
var_name.toUpperCase()

//for making your sring in lowercase
var_name.toLowerCase()

//for removing gibris from back of your number
var_name.parseInt()
var_name.parseFloat()

//if you wana push or add somethingh to array
arry_name.push("xyz")
 xyz will get added into arry 

 arry_name.pop()
 this remove last thing from the array 
put any value in it will place that in place of value you are removing 


 arry_name.shif()
 it remove first from the array
 this remove last thing from the array 
put any value in it will place that in place of value you are removing 



arry_name.concat()
same as the.push but there is slight diffrence that concat push the array inside of the array 

array_name.forEach(name_function)
.forEach is an function which takes an array and call other function and pass values of array one by one in the selected function / thats why is called as foreach simply call the selected function one by one foreach element of array  



 